# 本项目将程杰出版的大话设计模式写为cpp，主要是加深自己对于设计模式的理解
- 本项目跳过了书中的部分程序，只保留重点代码
- 本项目结构会与书中的目录对应，例如1.3对应了书中的第1.3节的代码规范
- 部分代码由AI编写

# 编译方式
```
cd DesignPatterns
cmake -B build
cmake --build build
```
然后在`DesignPatterns/build`中可以看到编译出来的本项目中的所有程序。